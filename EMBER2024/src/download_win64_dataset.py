import thrember

thrember.download_dataset("dataset", file_type="Win64", split="train")
# thrember.download_dataset("dataset", file_type="Win64", split="test") -> DONE
# thrember.download_dataset("dataset", split="challenge") -> DONE